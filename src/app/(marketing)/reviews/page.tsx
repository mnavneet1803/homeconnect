import { notFound } from "next/navigation";

/**
 * TODO: Re-enable this page once Google reviews / verified testimonials are available.
 * Previous implementation lived here (featured testimonials + GoogleReviewsBadge + embed slot).
 */
export default function ReviewsPage() {
  notFound();
}
